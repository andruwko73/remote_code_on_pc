# Remote Code on PC

Remote Code on PC связывает Android-приложение с расширением VS Code на Windows-ПК. Телефон становится удаленным интерфейсом к рабочему окну VS Code: можно писать в единый чат Remote Code Agent, прикреплять файлы и скриншоты, просматривать историю, открывать файлы проекта, запускать действия после подтверждения и получать потоковые ответы через WebSocket.

Проект сейчас состоит из двух основных частей:

- `extension/` - расширение VS Code с HTTP/WebSocket сервером, webview-чатом и Remote Code Agent.
- `android/` - Android APK на Kotlin/Jetpack Compose.

Актуальные артефакты:

- APK: `apk/app-debug.apk`, версия `1.0.65`.
- VSIX: `extension/remote-code-on-pc-1.1.73.vsix`, версия `1.1.73`.
- APK raw URL: https://raw.githubusercontent.com/andruwko73/remote_code_on_pc/main/apk/app-debug.apk
- VSIX raw URL: https://raw.githubusercontent.com/andruwko73/remote_code_on_pc/main/extension/remote-code-on-pc-1.1.73.vsix
- Порт по умолчанию: `8799`.

Последняя проверка: `2026-05-05`.

- Расширение: `npm run compile`, `npm test`, `npx vsce package --allow-missing-repository`, установка VSIX `1.1.73`; активное окно VS Code до reload еще может показывать старый `serverVersion`.
- API расширения: `/api/status`, `/api/codex/models`, `/api/codex/threads`, `/api/tunnel/status` отвечают `200`.
- Кнопки webview и APK: `npm test` проверяет `144` пункта, включая `data-action`, `approve/deny`, hover-кнопки сообщений, карточку изменений `undo/review/toggle`, `Ctrl+V`, `Enter`, `stop`, стартовые кнопки APK, явную кнопку/команду токена, меню копирования/создания нового токена, автоматический UPnP-проброс порта, автоматическую и ручную настройку Keenetic, маскировку токена в APK, обязательный токен внешней сети, нормализацию публичного URL, WebSocket-защиту внешнего доступа, меню текущего чата, вложения, голос и синхронизацию настроек composer.
- Android-чат нормализует историю после optimistic-send, WebSocket и `/api/codex/history`, чтобы одно сообщение с телефона не отображалось дублем на APK.
- Внешняя сеть: APK не подключается к публичному URL без токена доступа, маскирует поле токена, принимает `http://...`, `https://...` и `//...`, а для Keenetic/KeenDNS URL без явного порта автоматически использует порт Remote Code. Если Keenetic/KeenDNS URL резолвится, но TCP-порт Remote Code недоступен или соединение обрывается до HTTP-ответа, приложение показывает отдельную ошибку. Для работы нужен прямой доступ/KeenDNS Direct и проброс TCP `8799 -> IP ПК:8799`; обычный облачный Keenetic URL без проброса произвольного TCP не заменяет этот маршрут.
- APK: `:app:lintDebug`, `:app:assembleDebug`, `:app:testDebugUnitTest` завершились успешно; unit-test исходников сейчас нет, поэтому HTML-отчет тестов не создается.
- Эмулятор: `RemoteCode_API34`, установка `apk/app-debug.apk`, запуск `com.remotecodeonpc.app/.MainActivity`, локальное подключение к VS Code, переходы `CODEX / ФАЙЛЫ / Настройки`, crash/FATAL в logcat не обнаружены.
- Кнопки APK: проверены навигация, локальное подключение, вкладка файлов, настройки; destructive-действия вроде удаления чатов/очистки логов/обновления не нажимались на живом состоянии, но их `onClick`-цепочки проверены статически.
- Итог по кнопкам: все видимые кнопки расширения должны иметь обработчик; все ключевые кнопки приложения имеют связанный `ViewModel`/API-вызов. Следующий этап можно начинать с UX-доводки и живых сценариев, а не с поиска "мертвых" кнопок.

После установки нового VSIX в уже открытом VS Code нужно выполнить `Developer: Reload Window`, иначе активное окно может продолжить работать на старой версии расширения.

## Что работает

### Проверка кнопок и действий

Расширение VS Code:

- верхняя панель: запуск/остановка задачи, выбор режима VS Code, терминал, layout/panel;
- меню текущего чата: новый чат, выбор чата, переименование, очистка, удаление, настройки;
- список чатов: выбор, создание, удаление, активный thread;
- composer: `+` для файла, вставка изображения через `Ctrl+V`, профиль, модель, effort, отправка по `Enter`, stop во время генерации;
- сообщения: hover-кнопки копирования, оценка вверх/вниз, раскрытие;
- карточка изменений: раскрытие файла, `Проверить`, `Отменить`, expand/collapse;
- карточки действий: approve/deny, отображение статуса, stdout/stderr;
- боковая область широкого режима: progress, сведения о ветке, Git, артефакты и ссылки API;
- все `data-action` в webview автоматически сверяются с `case`-обработчиками в `npm test`.

Android APK:

- стартовый экран: подключение, отправка логов, очистка логов, обновление из GitHub;
- настройки: локальная сеть, внешний URL, токен, запуск/сохранение подключения, проверка, отключение;
- верхняя навигация: `CODEX`, `ФАЙЛЫ`, настройки;
- чат: выбор истории, удаление чата, новый чат, меню текущего thread;
- composer: вложения, голосовой ввод, профиль/модель/effort, отправка, stop;
- сообщения: compact Codex-like layout, дедупликация истории, auto-scroll к нижнему anchor;
- действия: approve/deny и обновление истории после ответа;
- файлы: дерево проекта, открытие/чтение файла.

Ограничения проверки: опасные действия, которые удаляют данные или меняют живое состояние (`удалить чат`, `очистить логи`, установка обновления поверх текущего APK), проверялись по цепочкам обработчиков и сборке, а не массовым нажатием на рабочем состоянии. Внешняя сеть зависит от реального роутера/Keenetic-проброса порта и должна проверяться на месте.

### Расширение VS Code

Расширение запускает локальный сервер и открывает окно `Remote Code`, похожее на чат Codex. Оно отвечает за связь с телефоном, хранение истории Remote Code, работу с VS Code Language Model API и выполнение подтвержденных действий.

Основные возможности:

- единый чат в VS Code и на телефоне;
- потоковая генерация ответа;
- история Remote Code-чатов и частичная загрузка найденных Codex Desktop session-файлов;
- выбор модели из Codex/OpenAI-совместимого списка;
- выбор усилия рассуждения: `Низкий`, `Средний`, `Высокий`, `Очень высокий`;
- профили работы: `Пользовательские`, `Проверка`, `Быстрый режим`;
- отправка файлов из VS Code через кнопку `+`;
- вставка файлов и изображений через `Ctrl+V` в composer webview;
- прием файлов и скриншотов с телефона;
- отправка изображений в модель как image data part, если это поддерживает выбранная модель VS Code;
- показ измененных файлов в карточке изменений;
- открытие файла из карточки изменений;
- открытие SCM/проверки изменений;
- запуск терминала VS Code;
- показ диагностики VS Code;
- подсказки реальной структуры проекта в контексте агента, чтобы `read-file` не уходил в несуществующие файлы;
- подтверждения действий `approve/deny` на ПК и телефоне;
- запуск терминальных команд только после подтверждения, кроме явно безопасных read-only команд в быстром профиле;
- запись/патч файлов только после подтверждения;
- кнопка остановки генерации во время активного ответа;
- внешний доступ через постоянный Keenetic/KeenDNS URL с проверкой `/api/status`;
- токен авторизации через настройку `remoteCodeOnPC.authToken` и меню `Remote Code on PC: Подключение`;
- публичные `/api/status` и `/api/tunnel/status` для диагностики подключения; при обращении через внешний Host без валидного токена они отдают только минимальный статус без IP, workspace и публичного URL;
- правая панель прогресса в webview: текущие шаги, сведения о ветке, Git-действия и артефакты подключения;
- live sync через WebSocket.

### Android APK

Приложение подключается к серверу расширения по локальной сети или по публичному Keenetic/KeenDNS URL. Интерфейс сделан компактно, ближе к Codex: верхняя навигация `CODEX / ФАЙЛЫ`, история чатов, composer с моделью, усилием, вложениями, голосовым вводом и кнопкой отправки/стоп.

Основные возможности:

- подключение по IP/порту;
- подключение через публичный URL;
- поддержка токена доступа;
- автообновление APK из GitHub;
- отправка логов и очистка логов;
- отображение версии APK, версии VS Code и версии расширения;
- чат Remote Code Agent;
- история чатов с удалением;
- создание нового чата кнопкой-карандашом;
- выбор модели и усилия;
- выбор профиля работы;
- включение/выключение IDE-контекста;
- отправка текста по кнопке отправки или через IME action клавиатуры;
- кнопка стоп во время генерации;
- прикрепление файлов и скриншотов;
- отправка вложений base64 в `/api/codex/send`;
- голосовой ввод через системный `RecognizerIntent`;
- отображение action-карточек с `Разрешить`/`Отклонить`;
- просмотр файлов проекта во вкладке `ФАЙЛЫ`;
- compact settings screen для локальной сети, внешней сети, токена, статуса авторизации и обновлений;
- карточки измененных файлов в чате с зелеными/красными счетчиками строк и открытием файла по нажатию.

## Ограничения и важные замечания ревью

Это собственный Remote Code Agent, а не встроенная копия официального Codex Desktop. Расширение использует VS Code Language Model API и собственные директивы действий:

- `::run-command{...}` - запрос запуска команды;
- `::read-file{...}` - запрос чтения файла;
- `::show-diagnostics{}` - запрос диагностики;
- `::write-file{...}` - запрос записи файла;
- `::apply-patch{...}` - запрос применения patch.

Из-за этого:

- модель не имеет всех внутренних инструментов настольного Codex автоматически;
- поведение approve/deny, diff, stdout/stderr и карточек действий реализовано в этом проекте отдельно;
- для полного Codex-уровня нужно дальше усиливать работу с diff, интерактивные изменения, tool-пайплайн и тестирование всех кнопок на реальном UI;
- после установки расширения всегда нужен reload активного окна VS Code;
- если телефон показывает старую ошибку туннеля или старый интерфейс, почти всегда активное окно VS Code еще не подхватило новый VSIX.

Риски ревью и их статус:

- Закрыто в `android/app/src/main/java/com/remotecodeonpc/app/RemoteCodeApp.kt`: удалена старая неиспользуемая `SettingsScreen`, из `SettingsScreenV2` убраны временный `if (true)`, `return` и недостижимый старый блок настроек. Осталась одна компактная реализация.
- Закрыто в `android/app/src/main/java/com/remotecodeonpc/app/viewmodel/MainViewModel.kt`: из `startTunnel()` удален старый недостижимый код, функция теперь явно делегирует запуск в `startTunnelImproved()`.
- Улучшено в `extension/src/server.ts`: запрос в модель теперь включает последние сообщения текущего thread, а не только текущее сообщение с workspace context. Новое сообщение не дублируется в истории prompt.
- Осталось как локальная операционная рекомендация: в `git remote` не стоит хранить токен в URL. Лучше использовать Git Credential Manager или обычный URL `https://github.com/andruwko73/remote_code_on_pc.git`.
- Закрыто как repo hygiene: локальные диагностические файлы (`extension/check_chat_data*.py`, `.remote-code-uploads/`, `extension/launch_log.txt` и соседние проверочные скрипты) добавлены в `.gitignore` и не коммитятся.

## Установка расширения

Из готового VSIX:

```powershell
code --install-extension C:\Users\Admin\Documents\git\remote_code_on_pc\extension\remote-code-on-pc-1.1.73.vsix --force
```
После установки:

1. Откройте VS Code.
2. Выполните `Developer: Reload Window`.
3. Откройте команду `Remote Code on PC: Open Chat`.
4. Убедитесь, что в статус-баре есть `Remote :8799` или сервер доступен по `http://IP-ПК:8799/api/status`.

## Установка APK

Готовый APK лежит здесь:

```text
C:\Users\Admin\Documents\git\remote_code_on_pc\apk\app-debug.apk
```

Его же приложение скачивает по кнопке `Обновить из GitHub`:

```text
https://raw.githubusercontent.com/andruwko73/remote_code_on_pc/main/apk/app-debug.apk
```

Если Android просит разрешение на установку из неизвестного источника, после выдачи разрешения нажмите обновление еще раз или вернитесь в приложение. Текущая логика должна продолжать установку, но поведение зависит от оболочки Android.

## Подключение по локальной сети

1. ПК и телефон должны быть в одной сети Wi-Fi.
2. В VS Code должно быть установлено и запущено расширение Remote Code on PC.
3. В приложении укажите IP ПК, например `192.168.x.x`.
4. Порт: `8799`.
5. Если токен в настройках расширения пустой, поле токена в приложении тоже оставьте пустым.
6. Нажмите `Подключиться`.

Проверка с ПК:

```powershell
curl.exe http://127.0.0.1:8799/api/status
```

Проверка с телефона: подключение должно открыть экран `CODEX`.

## Подключение через внешнюю сеть

Внешний доступ нужен, если телефон не в одной сети с ПК.

Основной сценарий внешней сети теперь Keenetic/KeenDNS. Расширение не запускает `cloudflared` и не выдает одноразовый `trycloudflare.com` адрес. Вместо этого расширение формирует и хранит постоянный публичный URL роутера:

```text
remoteCodeOnPC.keeneticHost = name.keenetic.link
remoteCodeOnPC.publicUrl = http://name.keenetic.link:8799
```

На Keenetic нужно настроить KeenDNS/публичный домен и проброс TCP-порта `8799` на IP ПК, где запущено расширение, например `192.168.x.x:8799`. Сервер расширения должен слушать `0.0.0.0:8799`.

Для прямого проброса порта к HTTP-серверу расширения обычно нужен `http://...:8799`. Если у вас на Keenetic настроен отдельный HTTPS-прокси, схему можно поменять в `remoteCodeOnPC.keeneticScheme`.

В VS Code:

1. Откройте команду `Remote Code on PC: Подключение`.
2. Выберите `Сформировать Keenetic URL`.
3. Если расширение не нашло KeenDNS через `my.keenetic.net` / домен Keenetic `netcraze.io`, введите короткое имя `name` или полный домен `name.keenetic.link`.
4. Расширение сохранит `remoteCodeOnPC.keeneticHost` и `remoteCodeOnPC.publicUrl`, скопирует готовый URL.
5. Создайте/скопируйте токен доступа. Для внешней сети `/api/tunnel/start` требует включенный токен, чтобы публичный адрес не работал без авторизации.
6. Если внешний URL резолвится, но `:8799` таймаутит, в том же меню выберите `Настроить Keenetic автоматически`. Расширение попросит локальный адрес роутера, логин/пароль администратора и имя WAN-интерфейса, затем через RCI создаст правило `TCP 8799 -> IP ПК:8799`. Пароль не сохраняется.
7. Если автоматическая настройка недоступна или нужно сделать правило руками, выберите `Открыть порт на роутере (UPnP)` либо `Скопировать инструкцию Keenetic`. UPnP сработает только если включен на Keenetic; инструкция положит в буфер обмена точные поля для веб-интерфейса и CLI-шаблон. Пункт `Открыть Keenetic` откроет `http://my.keenetic.net/`.

Быстрый способ создать или заменить токен: в VS Code откройте Command Palette (`Ctrl+Shift+P`) и выполните `Remote Code on PC: Токен доступа`. В webview Remote Code также есть подписанная кнопка `Токен` с замком в верхней панели. Если токен уже есть, меню предлагает два разных действия: `Скопировать текущий токен` и `Создать новый токен`. При создании нового старый токен сразу перестает работать, поэтому новый нужно вставить в APK и снова сохранить/подключиться.

`/api/tunnel/status` и `/api/status` отдают этот URL как `provider: keenetic` только авторизованному клиенту. Без токена они возвращают минимальный статус `authRequired/authOk`, чтобы APK мог показать понятную ошибку, но не раскрывал IP, workspace и публичный URL. `/api/tunnel/start` больше не запускает внешний процесс: он возвращает сохраненный адрес, собирает URL из `remoteCodeOnPC.keeneticHost` или пытается определить KeenDNS через `my.keenetic.net`; если токен не создан или URL сформировать невозможно, возвращает понятную ошибку.

В приложении:

1. На стартовом экране или в настройках включите `Внешняя сеть`.
2. Вставьте публичный Keenetic URL или, если телефон подключен локально, нажмите `Сформировать`, чтобы расширение само собрало и вернуло Keenetic URL.
3. Вставьте токен доступа. Для внешней сети токен обязателен: его нужно создать в VS Code через `Remote Code on PC: Подключение`.
4. Нажмите `Подключиться` или `Сохранить`/`Проверить`.

Для ручной проверки с ПК или другого устройства:

```powershell
curl.exe -I http://name.keenetic.link:8799/api/status
```

В приложение нужно вставлять URL вида:

```text
http://name.keenetic.link:8799
```

## Токен доступа

Токен задается в настройках VS Code:

```text
remoteCodeOnPC.authToken
```

Если токен задан:

- HTTP-запросы должны идти с `Authorization: Bearer <token>`;
- WebSocket подключается с тем же токеном;
- в приложении нужно вставить тот же токен в поле `Токен доступа`; поле скрывает секрет по умолчанию, открыть его можно только кнопкой просмотра.

Если токен пустой, приложение может подключаться без авторизации только в локальной сети. Для внешней сети расширение требует создать токен; внешний HTTP/WebSocket доступ без токена получает только минимальный статус или отказ, а APK не начинает внешнее подключение до ввода токена.

## Приватность логов

Логи APK и расширения автоматически маскируют:

- `Authorization: Bearer ...` и query-параметр `token=...`;
- приватные IP вида `192.168.x.x`, `10.x.x.x`, `172.16-31.x.x`;
- публичные URL провайдеров `trycloudflare.com`, `netcraze.io` и `keenetic.*`.

Экран приложения может показывать адрес подключения для удобства, но файл логов для отправки должен уходить без секретов и конкретных сетевых адресов.

## Интерфейс расширения

Верхняя панель:

- карандаш - создать новый чат;
- название чата - открыть список чатов;
- стрелка рядом с названием - раскрыть список чатов;
- `...` - меню текущего чата;
- play - запустить/проверить работу Remote Code;
- `VS Code` - меню действий VS Code;
- терминал - открыть терминал VS Code;
- layout - переключить панель/auxiliary bar.

Меню текущего чата:

- `Переименовать чат`;
- `Очистить чат`;
- `Удалить чат`;
- `Настройки`.

Меню `VS Code`:

- `Диагностика`;
- `Изменения Git`;
- `Терминал`.

Область сообщений:

- сообщения пользователя отображаются справа/плашкой;
- ответы агента отображаются как обычный текст;
- модель и effort выводятся под ответом;
- inline-код подсвечивается серыми плашками;
- карточка изменений показывает список файлов и `+/-`;
- по строке файла можно открыть файл;
- `Проверить` открывает SCM/изменения;
- hover-кнопки сообщения: копировать, оценка вверх, оценка вниз, раскрытие.

Composer:

- `+` - прикрепить файлы;
- шестеренка - профиль работы;
- модель - выбор модели;
- effort - выбор усилия;
- микрофон/голос - пока системная функция доступнее в Android APK, в webview VS Code ограничена окружением Electron;
- круглая стрелка - отправка;
- во время генерации кнопка становится стопом;
- `Enter` отправляет сообщение;
- `Shift+Enter` переносит строку;
- `Ctrl+V` вставляет файл/скриншот, если VS Code webview передает файл из буфера.

## Интерфейс Android-приложения

Экран подключения:

- `IP-адрес ПК` - адрес компьютера в локальной сети;
- `Порт` - обычно `8799`;
- `Токен` - необязательный, нужен если включен в расширении;
- `Подключиться`;
- `Отправить логи`;
- `Очистить логи`;
- `Обновить из GitHub`;
- версия APK под заголовком.

Экран `CODEX`:

- верхняя вкладка `CODEX` - чат агента;
- верхняя вкладка `ФАЙЛЫ` - файлы проекта;
- шестеренка - настройки;
- название чата - текущий thread;
- `...` - история чатов;
- карандаш - новый чат;
- сообщение и ответы синхронизируются с расширением через WebSocket;
- карточки подтверждений появляются при запросе действий;
- `Разрешить` запускает действие на ПК;
- `Отклонить` отменяет действие.

Composer на телефоне:

- `+` - прикрепить файл/скриншот;
- шестеренка - профиль;
- `GPT-5.5 / Высокий` - модель и effort;
- sparkle - IDE context;
- микрофон - голосовой ввод Android;
- круглая кнопка - отправить или остановить генерацию.

Настройки:

- статус подключения;
- режим: локальная сеть или внешняя сеть;
- версия APK;
- версия VS Code-сервера;
- IP ПК;
- блок локальной сети;
- блок внешней сети с публичным Keenetic URL;
- получение Keenetic URL из расширения;
- токен доступа;
- переподключение;
- обновление APK;
- логи.

## API расширения

Основные endpoint:

```text
GET  /api/status
GET  /api/workspace/folders
POST /api/workspace/open
GET  /api/workspace/tree
GET  /api/workspace/read-file
GET  /api/diagnostics
POST /api/terminal/exec

GET  /api/codex/status
POST /api/codex/send
GET  /api/codex/history
GET  /api/codex/events
GET  /api/codex/actions
POST /api/codex/actions
GET  /api/codex/models
POST /api/codex/models
GET  /api/codex/threads
POST /api/codex/new
POST /api/codex/delete
POST /api/codex/stop
POST /api/codex/launch

GET  /api/tunnel/status
POST /api/tunnel/start
POST /api/tunnel/stop

WS   /ws
```

## Сборка и проверки

Расширение:

```powershell
cd C:\Users\Admin\Documents\git\remote_code_on_pc\extension
npm install
npm run compile
npx vsce package --allow-missing-repository
code --install-extension .\remote-code-on-pc-1.1.73.vsix --force
```

APK:

```powershell
cd C:\Users\Admin\Documents\git\remote_code_on_pc\android
.\gradlew.bat :app:assembleDebug
.\gradlew.bat :app:lintDebug
```

Копирование APK в папку обновления:

```powershell
Copy-Item C:\Users\Admin\Documents\git\remote_code_on_pc\android\app\build\outputs\apk\debug\app-debug.apk C:\Users\Admin\Documents\git\remote_code_on_pc\apk\app-debug.apk -Force
```

Проверка raw-ссылок:

```powershell
curl.exe -L -I https://raw.githubusercontent.com/andruwko73/remote_code_on_pc/main/apk/app-debug.apk
curl.exe -L -I https://raw.githubusercontent.com/andruwko73/remote_code_on_pc/main/extension/remote-code-on-pc-1.1.73.vsix
```

Проверка на эмуляторе:

```powershell
adb install -r C:\Users\Admin\Documents\git\remote_code_on_pc\apk\app-debug.apk
adb shell monkey -p com.remotecodeonpc.app 1
adb logcat -d | Select-String -Pattern "FATAL|AndroidRuntime|RemoteCode"
```

## Что проверять после каждой доработки

Расширение:

- открывается окно `Remote Code`;
- сервер отвечает на `/api/status`;
- список чатов открывается;
- новый чат создается карандашом;
- чат можно удалить;
- отправка по `Enter`;
- `Shift+Enter` переносит строку;
- во время генерации кнопка отправки становится стопом;
- стоп реально отменяет текущую генерацию;
- выбор модели сохраняется;
- выбор effort сохраняется;
- профиль сохраняется;
- `+` прикрепляет файл;
- `Ctrl+V` прикрепляет картинку из буфера;
- карточка изменений рендерится компактно;
- файл из карточки открывается;
- hover-кнопка копирования пишет в clipboard;
- лайк/дизлайк сохраняется;
- `Диагностика`, `Git`, `Терминал`, `Настройки` открывают соответствующие окна VS Code;
- action approval появляется в VS Code и на телефоне;
- approve запускает действие;
- deny отменяет действие.

Приложение:

- открывается без crash;
- подключается по локальной сети;
- показывает версию APK и VS Code;
- подключается с токеном;
- подключается через публичный URL;
- кнопка `Сформировать` просит расширение сформировать/вернуть Keenetic URL или показывает понятную ошибку;
- история чатов грузится;
- удаление чата работает;
- карандаш создает новый чат;
- текст не обрезается в списке чатов;
- `+` быстро открывает выбор файла;
- скриншот отправляется как вложение;
- модель видит изображение;
- голосовой ввод вставляет текст;
- кнопка отправки меняется на стоп;
- approve/deny видны на телефоне;
- обновление из GitHub скачивает свежий APK;
- очистка логов очищает локальный лог.

## Готовый запрос для продолжения работы с телефона

Этот текст можно отправить в чат Remote Code с телефона, чтобы следующая сессия понимала состояние проекта и продолжила разработку расширения и APK:

```text
Продолжи работу над проектом Remote Code on PC в папке C:\Users\Admin\Documents\git\remote_code_on_pc.

Цель проекта: Android-приложение должно быть удаленным интерфейсом к VS Code на ПК. Расширение VS Code должно давать Codex-похожий чат Remote Code Agent: единый чат в VS Code и на телефоне, live sync через WebSocket, потоковая генерация, история чатов, выбор модели/effort/профиля, работа с файлами проекта, прикрепление файлов и скриншотов с телефона, запуск команд и изменение файлов только после подтверждения пользователя на ПК или телефоне.

Текущие версии:
- Android APK: 1.0.65, файл apk/app-debug.apk.
- VS Code extension: 1.1.73, файл extension/remote-code-on-pc-1.1.73.vsix.
- Сервер расширения слушает порт 8799.
- После установки VSIX нужно сделать Developer: Reload Window.

Как устроено расширение:
- extension/src/server.ts запускает HTTP/WebSocket сервер.
- Главный чат находится в webview Remote Code.
- POST /api/codex/send принимает сообщение, модель, effort, профиль, threadId и attachments.
- Вложения с телефона сохраняются в .remote-code-uploads и передаются модели; изображения должны уходить как image data part.
- Ответ модели идет через VS Code Language Model API и стримится в VS Code webview и Android через WebSocket.
- История Remote Code хранится в VS Code globalState.
- Список чатов должен показывать Remote Code threads и найденные Codex Desktop session-файлы.
- Действия запрашиваются директивами ::run-command, ::read-file, ::show-diagnostics, ::write-file, ::apply-patch.
- Подтверждения должны отображаться на ПК и телефоне, approve запускает действие, deny отменяет.
- Карточка изменений должна выглядеть как в Codex: компактная шапка "Изменено N файлов +X -Y", строки файлов, раскрытие, кнопка "Проверить", открытие файла по клику.
- Кнопки webview должны работать: новый чат, список чатов, удалить чат, переименовать, очистить, настройки, диагностика, Git/SCM, терминал, layout, + file, Ctrl+V для картинки, профиль, модель, effort, отправить, стоп.

Как устроено Android-приложение:
- android/app/src/main/java/com/remotecodeonpc/app/RemoteCodeApp.kt содержит навигацию и настройки.
- android/app/src/main/java/com/remotecodeonpc/app/ui/screens/CodexScreen.kt содержит чат, список чатов, composer, вложения, voice input, карточки действий и изменений.
- android/app/src/main/java/com/remotecodeonpc/app/viewmodel/MainViewModel.kt отвечает за подключение, WebSocket, отправку сообщений, модели, threads, actions, tunnel, update.
- android/app/src/main/java/com/remotecodeonpc/app/network/ApiClient.kt содержит Retrofit API.
- android/app/src/main/java/com/remotecodeonpc/app/network/WebSocketClient.kt содержит WebSocket reconnect.

Интерфейс Android:
- Экран подключения: переключатель локальная/внешняя сеть, IP ПК и порт для LAN, публичный Keenetic URL для внешней сети, токен, подключиться, отправить логи, очистить логи, обновить из GitHub, версия APK.
- Верхняя навигация после подключения: CODEX, ФАЙЛЫ, настройки.
- В чате: заголовок текущего thread, кнопка истории, карандаш нового чата, сообщения, action-карточки, composer.
- Composer: + вложения, профиль, модель+effort, IDE context, микрофон, отправить/стоп.
- Настройки должны быть компактными: статус, режим, провайдер Keenetic, APK, расширение, VS Code, IP, порт, public URL, получение URL из расширения, токен, переподключение, обновление, логи.

Что обязательно проверить:
1. npm run compile в extension.
2. npx vsce package --allow-missing-repository.
3. Установить VSIX в VS Code и сделать Developer: Reload Window.
4. ./gradlew.bat :app:assembleDebug.
5. ./gradlew.bat :app:lintDebug.
6. Установить APK на эмулятор или телефон.
7. Проверить подключение к http://IP-ПК:8799.
8. Проверить отправку сообщения с телефона и из расширения.
9. Проверить, что ответ стримится на оба устройства.
10. Проверить прикрепление скриншота с телефона и Ctrl+V картинки в расширении.
11. Проверить approve/deny на ПК и телефоне.
12. Проверить кнопку стоп.
13. Проверить историю и удаление чатов.
14. Проверить внешний доступ через Keenetic/KeenDNS URL.
15. Проверить обновление APK из GitHub.

Что уже закрыто по ревью:
- RemoteCodeApp.kt: удалены старая SettingsScreen, if (true), return и мертвый fallback UI в SettingsScreenV2.
- MainViewModel.kt: удален недостижимый старый код startTunnel().
- extension/src/server.ts: в запрос модели добавлена история текущего thread без дублирования нового сообщения.

Что еще можно улучшать дальше:
- Довести UI webview и Android до максимально близкого вида Codex: размеры, шрифты, отступы, меню, карточки изменений, hover-меню.
- Сделать все кнопки либо рабочими, либо убрать их из UI.
- Проверить реальные approve/deny сценарии на телефоне и ПК после установки APK/VSIX.
- Не коммитить локальные .remote-code-uploads, launch_log и диагностические временные скрипты; они уже добавлены в .gitignore.

После изменений собери APK и VSIX, обнови apk/app-debug.apk, установи VSIX локально, проверь сборки, сделай commit и push в GitHub.
```
